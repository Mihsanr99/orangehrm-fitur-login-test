describe('Dashboard Page', () => {
//   beforeEach(() => {
    // cukup panggil custom command login
    // cy.login()
//   })

  it('Menampilkan Dashboard setelah login', () => {
    cy.login()
//     cy.get("h6.oxd-text--h6").should('have.text', 'Dashboard')
// //   })

// //   it('Menampilkan menu Quick Launch Assign Leave', () => {
//     cy.contains('Assign Leave').should('be.visible')
  })
})
